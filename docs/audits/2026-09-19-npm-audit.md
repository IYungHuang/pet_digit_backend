# npm audit — 2026-09-19

Command: `cd functions && npm audit --json`

Result: 21 findings — 13 moderate, 6 high, 2 critical.

Production-only command: `npm audit --omit=dev --json`

Result: 9 moderate, 0 high, 0 critical.

## Remediation

- Moved Firebase web client SDK to `devDependencies`; it exists only for local
  Rules tests and never enters Functions runtime deployment.
- Kept `firebase-admin@13` and `firebase-functions@6`. npm only offered the
  production remediation through `firebase-admin@14.4.0` and
  `firebase-functions@7.4.0`, both major upgrades outside this hardening pass.
- Kept `@firebase/rules-unit-testing@2` with Firebase 9 because current Rules
  test compat path depends on that pair. npm's suggested `5.0.2` requires a
  major SDK migration.
- Kept Vitest 2 because npm's fix is Vitest 5, a major test-runner upgrade.
  Vitest runs in non-UI mode only; no Vitest UI server is exposed.

Remaining findings are transitive or dev-only and require major upgrades for
the currently supported local scaffold. Re-run audit before production work;
production deployment must not proceed until Admin/Functions dependency
upgrades are tested and reviewed.

## Hardening pass recheck

On 2026-09-19T07:49:25Z, `npm audit --omit=dev --json` was retried. npm
registry DNS resolution failed (`getaddrinfo ENOTFOUND registry.npmjs.org`), so
no new audit result is claimed and no dependency was changed. Existing
classification above remains the last valid registry result; rerun when
registry access is available.
